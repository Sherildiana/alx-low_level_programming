#include <stdio.h>
/**
 * main -  a program that prints the alphabet in lowercase, followed by a new line.
 *
 * Return: always o (success)
*/

int main(void)
{
	char c;

	c = 'n';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
